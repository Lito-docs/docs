---
title: List Posts
description: "Retrieve a paginated list of all blog posts with optional filtering by author, status, and publication date"
api: GET /posts
baseUrl: https://jsonplaceholder.typicode.com
layout: '@/layouts/APIEndpointLayout.astro'
keywords:
  - list posts api
  - get posts endpoint
  - blog posts api
author: Lito Team
publishDate: "2026-01-04"
modifiedDate: "2026-02-10"
tags:
  - api
  - posts
section: API Reference
---

## Overview

Returns a list of all blog posts. Supports filtering by user ID and pagination.

## Query Parameters

<ParamTable type="query">
  <Param name="userId" type="integer">
    Filter posts by a specific user ID
  </Param>
  <Param name="_limit" type="integer" default="10">
    Maximum number of posts to return
  </Param>
  <Param name="_start" type="integer" default="0">
    Number of posts to skip for pagination
  </Param>
</ParamTable>

## Response

Returns an array of post objects.

<ResponseField name="id" type="integer">
  Unique identifier for the post
</ResponseField>

<ResponseField name="userId" type="integer">
  ID of the user who created the post
</ResponseField>

<ResponseField name="title" type="string">
  Title of the post
</ResponseField>

<ResponseField name="body" type="string">
  Content of the post
</ResponseField>

## Filtering Examples

### Get posts by a specific user

```bash
GET /posts?userId=1
```

### Paginate results

```bash
GET /posts?_start=10&_limit=5
```

<RequestExample>
<Tabs items={["cURL", "JavaScript", "Python"]}>
  <Tab title="cURL">
    ```bash
    curl -X GET "https://jsonplaceholder.typicode.com/posts?_limit=10"
    ```
  </Tab>
  <Tab title="JavaScript">
    ```javascript
    const response = await fetch("https://jsonplaceholder.typicode.com/posts?_limit=10");
    const data = await response.json();
    console.log(data);
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import requests

    response = requests.get(
        "https://jsonplaceholder.typicode.com/posts",
        params={"_limit": 10}
    )
    print(response.json())
    ```
  </Tab>
</Tabs>
</RequestExample>

<ResponseExample>
<Tabs items={["200"]}>
  <Tab title="200">
    ```json
    [
      {
        "userId": 1,
        "id": 1,
        "title": "sunt aut facere repellat provident occaecati excepturi optio reprehenderit",
        "body": "quia et suscipit\nsuscipit recusandae consequuntur expedita..."
      },
      {
        "userId": 1,
        "id": 2,
        "title": "qui est esse",
        "body": "est rerum tempore vitae\nsequi sint nihil reprehenderit dolor..."
      }
    ]
    ```
  </Tab>
</Tabs>
</ResponseExample>
